# HTTP target port for the backend server (localhost only).
# バックエンド(ローカルhost固定)のHTTPポート。
EXAMPLE_PROXY_TARGET_PORT=4122
# HTTPS listen port for the local proxy.
# ローカルプロキシのHTTPS待受ポート。
EXAMPLE_PROXY_HTTPS_PORT=4123
