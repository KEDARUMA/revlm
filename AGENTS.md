# AGENTS

## 目的
チャットがリセットされても作業を継続できるように、このプロジェクトの運用ルールと技術前提を保存する。

## 用語
- ユーザ: 作業を依頼・指示する本人。
- 改修開始: コード変更を許可する明示指示（この文言を含むこと）。
- ループ開始: ループワークフローの開始を許可する明示指示（この文言を含むこと）。

## 必須ルール
- チャットは日本語で、簡潔に返答する。
- 指示が曖昧な場合は、着手前に確認する。
- 「質問ある?」と聞かれた場合は、作業を進めず確認質問のみ返す。
- 対象ディレクトリを毎回明示し、monorepo の無関係領域は触らない。
- コード参照時は `ファイルパス:行番号` 形式で示す。
- 変更時は最小差分を原則とし、不要なリファクタリングをしない。
- コメントは簡潔に、英語、日本語の順で記載すること。
  例:
    // this comment.
    // これはコメントです。
- ログが提示されたら、事象・原因・対処を整理して返す。
- シェルコマンドのタイムアウトは原則 3 時間以内とする。

## 禁止ルール
- 「改修開始」指示がない状態でコード変更しない。
- Git の書き込み操作をしない（`commit` / `push` / `branch` 作成・削除など）。
- ユーザ確認なしで、ファイル変更を伴うコマンドを実行しない。

## 作業ログ運用ルール
- ファイル変更を行った場合は、同一セッション内で `AGENTS.LOG.md` に必ず記録する。
- 記録項目は `日時 / 変更ファイル / 変更概要 / 実行コマンド（必要時）` を含める。
- 複数ファイルを変更した場合は、ファイルごとに記録する。
- 事後追記ではなく、変更完了直後に追記する。

## 実行フロー
1. 指示を要約し、対象ディレクトリと実施内容を明示する。
2. 不明点があれば先に確認する。
3. 改修が必要な場合、改修方針を先に提示する。
4. 「改修開始」指示を受けてから編集する。
5. 変更後は結果を簡潔に報告し、必要なら次の確認事項を示す。

## ループワークフロー
- 「ループ開始」指示がある場合のみ実行できる。
- ループ実行中は「改修開始」なしで改修してよい。
- 問題発生時や判断が必要な時点でループを停止し、状況を報告する。
- ループ回数上限は 30 回。
- ループ終了時は、実行回数と結果を箇条書きで報告する。
- ループ終了後は通常ルールに戻り、改修には再度「改修開始」が必要。

## 作業フロー（エージェント運用）
1. 要件理解
2. 不明点確認（必要時のみ）
3. 改修方針の提示
4. ユーザーの「改修開始」待ち
5. 合意範囲のみ実装
6. 検証（ビルド・型・必要テスト）
7. 変更点と確認ポイントを簡潔に報告

## プロジェクトの概要
モノレポ (monorepo) を使用し、次の3つのパッケージで構成されている

### 使用技術
